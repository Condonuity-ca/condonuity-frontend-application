import { NavigationMonitorService } from './navigation-monitor.service';

describe('NavigationMonitor', () => {
  it('should create an instance', () => {
    expect(new NavigationMonitorService()).toBeTruthy();
  });
});
